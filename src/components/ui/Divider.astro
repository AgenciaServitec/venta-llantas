---
interface Props {
  fullScreen?: boolean;
}

const { fullScreen = false } = Astro.props;
---

<hr class={`divider ${fullScreen && "full-screen"}`} />

<style lang="scss">
  hr {
    width: 80%;
    background: #f40f0f;
    height: 5px;
    margin: 1em auto;
    border: none;
  }

  .full-screen {
    width: 100%;
  }
</style>
